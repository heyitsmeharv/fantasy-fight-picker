export const getEventId = (event) => event?.eventId ?? event?.id ?? null;
export const getFightId = (fight) => fight?.fightId ?? fight?.id ?? null;
export const getFighterId = (fighter) => fighter?.fighterId ?? fighter?.id ?? null;
